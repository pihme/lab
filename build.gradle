apply plugin: 'eclipse'

allprojects {
    apply plugin: 'java'
    apply plugin: 'eclipse'
}

subprojects {
        
    repositories {
        mavenCentral()        
    }
        
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.7' 
}
